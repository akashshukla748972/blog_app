<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("./partials/head") %>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <%- include("./partials/nav") %>
    <div class="container p-2">
      <% if (locals.message){ %>

      <div class="alert alert-danger col-md-5 mx-auto" role="alert">
        <%= message %>
      </div>

      <% } %> <% if (locals.error){ %>

      <div class="alert alert-danger col-md-5 mx-auto" role="alert">
        <%= error %>
      </div>

      <% } %>
      <form
        action="/users/signin"
        method="post"
        class="form col-md-3 border border-2 position-fixed top-50 start-50 translate-middle d-flex flex-column gap-4 px-4 py-5"
      >
        <div class="">
          <h1 class="text-center fs-2 text-muted">Sign In</h1>
        </div>
        <div class="form-group">
          <label for="">Email</label>
          <input
            type="text"
            class="form-control"
            placeholder="eg.example.com"
            name="email"
            autofocus
          />
        </div>
        <div class="form-group">
          <label for="">Password</label>
          <input
            type="password"
            class="form-control"
            name="password"
            placeholder="eg.password"
          />
        </div>
        <div class="form-group">
          <button class="form-control btn btn-primary fw-semibold">
            Sign In
          </button>
        </div>
        <div class="">
          <p class="text-center text-muted">
            Don't have account?
            <a href="/users/signup" class="text-decoration-none fw-semibold"
              >sign up</a
            >
          </p>
        </div>
      </form>
    </div>
    <%- include("./partials/script") %>
  </body>
</html>
