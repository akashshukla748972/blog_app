<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("./partials/head") %>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <%- include("./partials/nav") %>
    <div class="container p-2">
      <% if (locals?.message){ %>

      <div class="alert alert-success col-md-5 mx-auto mb-2" role="alert"><%= message %></div>

      <% } %>
    <div class="container">
      <% if (locals?.error){ %>

      <div class="alert alert-danger col-md-5 mx-auto mb-2" role="alert"><%= error %></div>

      <% } %>
      <form
        action="/users/signup"
        method="post"
        class="form col-md-3 border border-2 position-absolute top-50 start-50 translate-middle d-flex flex-column gap-3 px-4 py-3"
      >
        <div class="">
          <h1 class="text-center fs-2 text-muted">Sign Up</h1>
        </div>
        <div class="form-group">
          <label for="">Full Name</label>
          <input
            type="text"
            class="form-control"
            placeholder="eg.Akash Shukla"
            name="full_name"
            autofocus
          />
        </div>
        <div class="form-group">
          <label for="">Email</label>
          <input
            type="text"
            class="form-control"
            placeholder="eg.akash@gmail.com"
            name="email"
          />
        </div>
        <div class="form-group">
          <label for="">Phone</label>
          <input
            type="text"
            class="form-control"
            placeholder="eg.0000000000"
            name="phone"
          />
        </div>
        <div class="form-group">
          <label for="">Password</label>
          <input
            type="password"
            class="form-control"
            placeholder="eg.Password"
            name="password"
          />
        </div>
        <div class="form-group">
          <button class="form-control btn btn-primary fw-semibold">
            Sign Up
          </button>
        </div>
        <div class="">
          <p class="text-center text-muted">
            Already have account?
            <a href="/users/signin" class="text-decoration-none fw-semibold"
              >sign in</a
            >
          </p>
        </div>
      </form>
    </div>
    <%- include("./partials/script") %>
  </body>
</html>
